import { ThemeConfig } from 'antd/es/config-provider/context';

const theme: ThemeConfig = {
  token: {
    colorPrimary: '#00b96b',
    fontSize: 16,
    borderRadius: 16,
    wireframe: true,
  },
};

export default theme;
